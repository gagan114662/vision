{
  "type": "object",
  "required": ["package_name"],
  "properties": {
    "package_name": {
      "type": "string",
      "description": "Name of the Python package to autonomously resolve and install"
    },
    "max_attempts": {
      "type": "integer",
      "description": "Maximum number of recovery strategies to attempt",
      "default": 5,
      "minimum": 1,
      "maximum": 10
    },
    "parallel_strategies": {
      "type": "boolean",
      "description": "Whether to execute multiple strategies in parallel for faster resolution",
      "default": false
    }
  },
  "additionalProperties": false
}