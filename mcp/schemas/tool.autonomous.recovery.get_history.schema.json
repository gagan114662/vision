{
  "type": "object",
  "properties": {
    "limit": {
      "type": "integer",
      "description": "Maximum number of recovery history records to return",
      "default": 50,
      "minimum": 1,
      "maximum": 500
    },
    "package_filter": {
      "type": "string",
      "description": "Optional filter to show history for a specific package only"
    },
    "success_only": {
      "type": "boolean",
      "description": "Whether to return only successful recovery attempts",
      "default": false
    }
  },
  "additionalProperties": false
}